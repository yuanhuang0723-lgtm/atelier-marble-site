"use client";

import { Suspense, useEffect, useMemo, useState } from "react";
import { DndContext, PointerSensor, closestCenter, useSensor, useSensors, type DragEndEvent } from "@dnd-kit/core";
import { SortableContext, arrayMove, verticalListSortingStrategy, useSortable } from "@dnd-kit/sortable";
import { CSS } from "@dnd-kit/utilities";
import { useRouter, useSearchParams } from "next/navigation";
import { z } from "zod";
import VisualPageRenderer from "../../../components/VisualPageRenderer";
import { getSupabaseBrowserClient } from "../../../lib/supabase/browser";
import { defaultHomeBlocks, type EditorBlock, type EditorDocument } from "../../../lib/visual-editor";

const blockPayload = z.array(z.object({ id: z.string(), type: z.string() }).passthrough()).min(1);
const pages = [{ label: "首页", path: "/", template: "home" as const }, { label: "工厂日常", path: "/factory", template: "factory" as const }, { label: "项目参考", path: "/projects", template: "project" as const }, { label: "关于我们", path: "/about", template: "article" as const }, { label: "联系页面", path: "/contact", template: "article" as const }];

function SortableBlock({ block, selected, onSelect }: { block: EditorBlock; selected: boolean; onSelect: () => void }) {
  const sortable = useSortable({ id: block.id });
  return <button ref={sortable.setNodeRef} style={{ transform: CSS.Transform.toString(sortable.transform), transition: sortable.transition }} {...sortable.attributes} {...sortable.listeners} onClick={onSelect} className={`flex w-full items-center gap-3 rounded-lg border px-3 py-3 text-left text-sm transition ${selected ? "border-[#bf6c35] bg-[#fff8f1]" : "border-ink/10 bg-white hover:border-ink/25"}`}><span className="cursor-grab text-ink/35">⋮⋮</span><span className="min-w-0 flex-1 truncate"><span className="block text-[10px] uppercase tracking-[0.14em] text-ink/45">{block.type}</span><span className="block truncate text-ink">{"title" in block ? block.title : "Content module"}</span></span>{selected ? <span className="text-[#bf6c35]">●</span> : null}</button>;
}

export default function VisualEditorPageEntry() {
  return <Suspense fallback={<main className="min-h-screen bg-[#f3eee6]" />}><VisualEditorPage /></Suspense>;
}

function newBlock(type: EditorBlock["type"]): EditorBlock {
  const id = `${type}-${crypto.randomUUID().slice(0, 8)}`;
  if (type === "hero") return { id, type, eyebrow: "New section", title: "Add a headline", body: "Add practical information for buyers.", image: "/materials/hero/atelier-marble-luxury-hero.webp", buttonLabel: "Learn more", buttonHref: "/contact" };
  if (type === "text") return { id, type, eyebrow: "Information", title: "Add a section title", body: "Add verified information here." };
  if (type === "card-grid") return { id, type, eyebrow: "References", title: "Add a card section", columns: 3, cards: [{ id: `${id}-card`, title: "New card", body: "Add a short description.", image: "/assets/home-top-cover.webp", href: "/contact" }] };
  if (type === "process") return { id, type, eyebrow: "Process", title: "Add a process", steps: [{ id: `${id}-step`, title: "Step one", body: "Add a short step description." }] };
  return { id, type: "cta", eyebrow: "Start a conversation", title: "Add a call to action", buttonLabel: "Contact us", buttonHref: "/contact" };
}

function VisualEditorPage() {
  const router = useRouter(); const searchParams = useSearchParams();
  const initialPage = pages.find((page) => page.path === searchParams.get("path")) || pages[0];
  const [currentPage, setCurrentPage] = useState(initialPage); const [locale, setLocale] = useState<"en" | "zh">("en"); const [blocks, setBlocks] = useState<EditorBlock[]>(defaultHomeBlocks); const [revision, setRevision] = useState(1); const [publishedRevision, setPublishedRevision] = useState<number | null>(null); const [selectedId, setSelectedId] = useState(defaultHomeBlocks[0].id); const [device, setDevice] = useState<"desktop" | "mobile">("desktop"); const [preview, setPreview] = useState(false); const [saveState, setSaveState] = useState("本地默认内容"); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }));
  const selected = useMemo(() => blocks.find((block) => block.id === selectedId) || blocks[0], [blocks, selectedId]);

  useEffect(() => { void getSupabaseBrowserClient().rpc("claim_first_admin").then(() => loadDocument(currentPage.path, locale)); }, [currentPage.path, locale]);
  useEffect(() => { if (!loading) { const timer = window.setTimeout(() => { void saveDraft(); }, 1000); return () => window.clearTimeout(timer); } }, [blocks]);

  async function loadDocument(path: string, nextLocale: "en" | "zh") {
    setLoading(true); setMessage("");
    try { const result = await getSupabaseBrowserClient().from("site_page_documents").select("draft_blocks,revision,published_revision").eq("path", path).eq("locale", nextLocale).maybeSingle(); if (result.error) throw result.error; const nextBlocks = Array.isArray(result.data?.draft_blocks) ? result.data.draft_blocks as EditorBlock[] : path === "/" && nextLocale === "en" ? defaultHomeBlocks : [newBlock("text")]; setBlocks(nextBlocks); setSelectedId(nextBlocks[0]?.id || ""); setRevision(result.data?.revision || 1); setPublishedRevision(result.data?.published_revision || null); setSaveState(result.data ? "已载入草稿" : "新页面草稿"); } catch (error) { setMessage(error instanceof Error ? error.message : "无法读取文档；当前使用本地草稿。"); setBlocks(path === "/" && nextLocale === "en" ? defaultHomeBlocks : [newBlock("text")]); setSelectedId(blocks[0]?.id || ""); } finally { setLoading(false); }
  }

  async function saveDraft() {
    const parsed = blockPayload.safeParse(blocks); if (!parsed.success) return setSaveState("内容需要检查");
    setSaveState("保存中…");
    const result = await getSupabaseBrowserClient().from("site_page_documents").upsert({ path: currentPage.path, locale, template: currentPage.template, draft_blocks: blocks, revision: revision + 1, updated_at: new Date().toISOString() }, { onConflict: "path,locale" }).select("revision").single();
    if (result.error) setSaveState("保存失败"); else { setRevision(result.data.revision); setSaveState("已保存草稿"); }
  }

  async function publish() {
    const parsed = blockPayload.safeParse(blocks); if (!parsed.success) return setMessage("请先修正内容结构。");
    setSaveState("发布中…"); const client = getSupabaseBrowserClient(); const result = await client.from("site_page_documents").update({ published_blocks: blocks, published_revision: revision, published_at: new Date().toISOString() }).eq("path", currentPage.path).eq("locale", locale); if (result.error) setMessage(result.error.message); else { setPublishedRevision(revision); setSaveState("已发布"); }
  }

  function updateSelected(patch: Partial<EditorBlock>) { if (!selected) return; setBlocks((current) => current.map((block) => block.id === selected.id ? { ...block, ...patch } as EditorBlock : block)); }
  function dragEnd(event: DragEndEvent) { const { active, over } = event; if (!over || active.id === over.id) return; setBlocks((current) => { const from = current.findIndex((block) => block.id === active.id); const to = current.findIndex((block) => block.id === over.id); return arrayMove(current, from, to); }); }
  function add(type: EditorBlock["type"]) { const block = newBlock(type); setBlocks((current) => [...current, block]); setSelectedId(block.id); }
  function openPage(page: typeof pages[number]) { setCurrentPage(page); router.replace(`/admin/editor?path=${encodeURIComponent(page.path)}`); }

  return <main className="flex h-screen min-h-[700px] flex-col overflow-hidden bg-[#f3eee6] text-ink"><header className="flex h-16 shrink-0 items-center justify-between border-b border-ink/10 bg-[#fbf8f2] px-4 md:px-6"><div className="flex items-center gap-4"><span className="font-title text-xl">Atelier Marble</span><span className="hidden rounded-full bg-stone px-3 py-1 text-xs text-ink/60 md:inline">可视化编辑器</span><span className="text-xs text-ink/45">/ {currentPage.label}</span></div><div className="flex items-center gap-2"><span className="mr-2 text-xs text-ink/55">{saveState}</span><button onClick={() => setLocale(locale === "en" ? "zh" : "en")} className="rounded-full border border-ink/15 px-3 py-2 text-xs">{locale === "en" ? "中文" : "English"}</button><button onClick={() => setDevice("desktop")} className={`rounded border px-3 py-2 text-xs ${device === "desktop" ? "bg-ink text-white" : "border-ink/15"}`}>电脑</button><button onClick={() => setDevice("mobile")} className={`rounded border px-3 py-2 text-xs ${device === "mobile" ? "bg-ink text-white" : "border-ink/15"}`}>手机</button><button onClick={() => setPreview(!preview)} className="rounded border border-ink/15 px-3 py-2 text-xs">{preview ? "编辑" : "预览"}</button><button onClick={() => void publish()} className="rounded-full bg-ink px-4 py-2 text-xs font-semibold text-white">发布</button></div></header><div className="flex min-h-0 flex-1"><aside className="hidden w-52 shrink-0 overflow-y-auto border-r border-ink/10 bg-[#f8f4ed] p-4 md:block"><p className="mb-3 text-[10px] font-semibold uppercase tracking-[0.18em] text-ink/45">页面</p><div className="grid gap-1">{pages.map((page) => <button key={page.path} onClick={() => openPage(page)} className={`rounded-lg px-3 py-3 text-left text-sm ${page.path === currentPage.path ? "bg-ink text-white" : "hover:bg-white"}`}>{page.label}</button>)}</div><div className="mt-8 border-t border-ink/10 pt-5"><p className="mb-3 text-[10px] font-semibold uppercase tracking-[0.18em] text-ink/45">模块大纲</p><DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={dragEnd}><SortableContext items={blocks.map((block) => block.id)} strategy={verticalListSortingStrategy}><div className="grid gap-2">{blocks.map((block) => <SortableBlock key={block.id} block={block} selected={selected?.id === block.id} onSelect={() => setSelectedId(block.id)} />)}</div></SortableContext></DndContext><select className="mt-4 w-full rounded-lg border border-ink/15 bg-white px-3 py-2 text-xs" value="" onChange={(e) => { if (e.target.value) add(e.target.value as EditorBlock["type"]); }}><option value="">＋ 添加模块</option><option value="text">图文介绍</option><option value="card-grid">卡片列表</option><option value="process">工作流程</option><option value="hero">主视觉</option><option value="cta">联系入口</option></select></div></aside><section className="min-w-0 flex-1 overflow-auto p-4 md:p-8"><div className={`mx-auto overflow-hidden rounded-xl border border-ink/10 bg-white shadow-xl transition-all ${device === "mobile" ? "max-w-[390px]" : "max-w-[1120px]"}`}><VisualPageRenderer blocks={blocks} selectedId={preview ? undefined : selected?.id} onSelect={preview ? undefined : setSelectedId} preview={preview} /></div></section><aside className="hidden w-80 shrink-0 overflow-y-auto border-l border-ink/10 bg-[#fbf8f2] p-5 lg:block"><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-ink/45">当前选中</p>{selected ? <><h2 className="mt-2 font-title text-2xl">{selected.type}</h2>{"eyebrow" in selected ? <label className="mt-6 block text-xs text-ink/60">眉题<input className="mt-2 w-full rounded border border-ink/15 bg-white p-3 text-sm" value={selected.eyebrow} onChange={(e) => updateSelected({ eyebrow: e.target.value })} /></label> : null}{"title" in selected ? <label className="mt-4 block text-xs text-ink/60">标题<textarea className="mt-2 min-h-20 w-full rounded border border-ink/15 bg-white p-3 text-sm" value={selected.title} onChange={(e) => updateSelected({ title: e.target.value })} /></label> : null}{"body" in selected ? <label className="mt-4 block text-xs text-ink/60">说明<textarea className="mt-2 min-h-28 w-full rounded border border-ink/15 bg-white p-3 text-sm" value={selected.body} onChange={(e) => updateSelected({ body: e.target.value })} /></label> : null}{selected.type === "hero" || selected.type === "cta" ? <label className="mt-4 block text-xs text-ink/60">按钮文字<input className="mt-2 w-full rounded border border-ink/15 bg-white p-3 text-sm" value={selected.buttonLabel} onChange={(e) => updateSelected({ buttonLabel: e.target.value })} /></label> : null}{selected.type === "hero" ? <label className="mt-4 block text-xs text-ink/60">图片地址<input className="mt-2 w-full rounded border border-ink/15 bg-white p-3 text-sm" value={selected.image} onChange={(e) => updateSelected({ image: e.target.value })} /></label> : null}{selected.type === "card-grid" ? <div className="mt-5 border-t border-ink/10 pt-4"><p className="text-xs text-ink/60">卡片顺序</p>{selected.cards.map((card, index) => <div key={card.id} className="mt-2 flex items-center gap-2 rounded border border-ink/10 bg-white p-2 text-xs"><span className="min-w-0 flex-1 truncate">{card.title}</span><button disabled={index === 0} onClick={() => updateSelected({ cards: arrayMove(selected.cards, index, index - 1) })} className="rounded border px-2 py-1 disabled:opacity-30">↑</button><button disabled={index === selected.cards.length - 1} onClick={() => updateSelected({ cards: arrayMove(selected.cards, index, index + 1) })} className="rounded border px-2 py-1 disabled:opacity-30">↓</button></div>)}</div> : null}</> : <p className="mt-4 text-sm text-ink/55">点击中间页面中的模块开始编辑。</p>}{message ? <p className="mt-6 rounded bg-red-50 p-3 text-xs text-red-700">{message}</p> : null}<div className="mt-8 border-t border-ink/10 pt-5 text-xs leading-6 text-ink/50">草稿会自动保存。发布前请先切换电脑和手机预览，确认图片比例和文字没有溢出。</div></aside></div></main>;
}
